{
  "name": "sua-airspace-vatpac-hosting",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Docker and PostgreSQL host for the VATPAC SUA Airspace site",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/db.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=22"
  }
}
